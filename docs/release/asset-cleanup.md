# 发布素材清理

构建仅排除已停用素材，public 中的原始文件保留。当前页面使用双帧高清动画、坐姿第 0/4 张（身体及眨眼）、环境和气泡图；这些图片未压缩或修改。姿态模型、全部 WASM 兼容版本、图标和许可证保留。

| 排除项 | 大小（MiB） |
|---|---:|
| 未使用的手部识别模型 | 7.5 |
| 已停用的三维角色模型 | 1.3 |
| 旧版静态动作海报 | 12.4 |
| 未使用的坐姿帧 | 17.4 |
| 旧版整张动画图集 | 10.6 |
| 旧版 v2 动画图集 | 5.4 |
| 已替换的高清单帧 | 28.1 |

当前源码完整打包 163.2 MiB，清理后 80.5 MiB，减少 82.6 MiB（69 个文件）。

排除规则位于 scripts/build.mjs。若以后重新启用旧渲染器或海报展示，须同步调整构建规则。完整清单见 ../../artifacts/package-cleanup/report.json。
