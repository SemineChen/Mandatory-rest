> 本文件保留此前素材阶段记录。最新布局、验证和未完成事项见 [视频接入与当前差异](./视频接入与当前差异.md)。

# 参考 UI 还原：素材与接入状态

2026-09-09。当前状态：素材阶段受阻，尚未修改休息页面或动作识别代码。

已生成 1536×1024 纯环境背景，位于 public/assets/garden/environment.png。角色、气泡、面板、摄像头与动态文字均已从背景中移除。原图中的桌面、植物、光线和主体构图作为生成约束，生成结果不等同于像素级复制。

使用内置 image_gen.imagegen 工具，共生成背景、角色、气泡、角色透明修正版、玻璃面板 5 个输出。工具未开放模型选择参数，因此不能确认底层模型为用户指定的 image2。全部透明资产候选经 PNG 头与系统元数据双重检查，均为 RGB（PNG color type 2），无 Alpha；棋盘格是图片内容。用户明确选择仅使用图片工具生成透明素材，故未做本地抠图、颜色键、混合模式或 CSS 遮罩来伪装透明。

透明 IP、无文字气泡、玻璃面板仍缺合格输出；装饰的生成暂缓，避免重复产生不可用的棋盘格。失败候选保存在 artifacts/ui-reference/generated-drafts，仅供追溯，不由页面引用。

分层与布局计划见同目录 JSON。待图片工具能输出实际透明素材后，继续组装、对齐截图、验证原有识别流程。现有扩展保持可运行版本；没有构建或视觉验收通过的声明。

## IP 视频制作约定（待实现）

- 所有状态使用同一透明画布、同一角色比例和脚底锚点。
- 分别提供待机、等待入镜、大鹏展翅、上下齐发、扭转乾坤、庆祝片段。
- 视频不包含气泡、文字、次数、面板或背景。
- 接入层将按现有 idle/confused/open/updown/twist/celebrate 状态选择片段，缺失或加载失败回退静态透明图。
- 视频播放层与摄像头分开；不会用 IP 视频替代摄像头识别数据。
- Alpha 编码、无缝循环和浏览器兼容性须用实际视频验证，目前尚无用户视频可测。
